%tr
  %td= link_to "#{worker.name}", worker_path(worker)
  %td= worker.woker_type
  -#%td= link_to "#{ca.client_name}", client_path(car.client)
  %td
    .btn-group
      %button.btn.btn-default.btn-xs.dropdown-toggle{"data-toggle" => "dropdown", :type => "button"}
        Menu
        %span.caret
      %ul.dropdown-menu{:role => "menu"}
        %li= link_to 'Show', worker_path(worker)
        %li= link_to 'Edit', edit_worker_path(worker)
        %li.divider
        %li= link_to 'Delete', worker_path(worker),
          method: :delete,
          data: { confirm: 'Are you sure?' },
          class: 'label-danger'


